apiVersion: v2
name: firezone
description: "Garuda Firezone stack (server + postgres + mss-clamp sidecar) with
  Gateway API HTTPRoute. Vanilla guest: accepts podLabels/podAnnotations from
  the garuda annotation layer; no FRR sidecar (removed Phase 4+5)."
type: application
version: 1.3.0
appVersion: "0.1.0"
